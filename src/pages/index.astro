---
import '../styles/global.css';
import Pillars from '../components/Pillars.astro';
import CodeSample from '../components/CodeSample.astro';

const title = 'Heirloom — Your enterprise domain model, as code.';
const description =
  'Heirloom is an open-source, self-hosted ontology layer: data, actions, logic, and security — defined once in TypeScript.';
const repoUrl = 'https://github.com/0xnicholas/heirloom-platform';
const specUrl = 'https://github.com/0xnicholas/heirloom-platform/tree/main/docs/spec';
const ogImageUrl = new URL('og.png', Astro.site);
const base = import.meta.env.BASE_URL;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href={`${base}favicon.svg`} type="image/svg+xml" />
    <link rel="icon" href={`${base}favicon.ico`} sizes="16x16 32x32" />
    <link rel="apple-touch-icon" href={`${base}apple-touch-icon.png`} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
  </head>
  <body>
    <header class="hero">
      <h1>Your enterprise domain model, as code.</h1>
      <p class="subhead">
        Heirloom is an open-source, self-hosted ontology layer: data, actions,
        logic, and security — defined once in TypeScript.
      </p>
      <div class="cta-row">
        <a class="cta-button" href={repoUrl}>View on GitHub</a>
        <a class="cta-ghost" href={specUrl}>Read the spec</a>
      </div>
    </header>

    <section id="pillars">
      <Pillars />
    </section>

    <section id="code-sample">
      <CodeSample />
    </section>

    <footer>
      <p>
        Heirloom is in design; the spec is public on{' '}
        <a href={repoUrl}>GitHub</a>. Apache-2.0.
      </p>
    </footer>
  </body>
</html>

<style>
  .cta-row {
    margin-top: 2rem;
    display: flex;
    gap: 0.9rem;
    align-items: center;
    flex-wrap: wrap;
  }
  .cta-button {
    display: inline-block;
    background: var(--accent);
    color: #fcfcfa;
    padding: 0.7rem 1.6rem;
    border-radius: 8px;
    font-size: 0.95rem;
  }
  .cta-button:hover {
    text-decoration: none;
    opacity: 0.92;
  }
  .cta-ghost {
    display: inline-block;
    border: 1px solid var(--ink);
    color: var(--ink);
    padding: 0.7rem 1.6rem;
    border-radius: 8px;
    font-size: 0.95rem;
  }
  .cta-ghost:hover {
    text-decoration: none;
    background: #ffffff;
  }
</style>
